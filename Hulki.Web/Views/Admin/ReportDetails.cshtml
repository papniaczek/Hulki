@model Hulki.Web.Models.DailyReport

@{
    ViewData["Title"] = "Szczegóły Wpisu";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Szczegóły Wpisu w Dzienniczku</h2>
        <a asp-action="PatientDetails" asp-route-id="@Model.AppUserId" class="btn btn-sm btn-custom-back">
            <i class="bi bi-arrow-left"></i> Wróć do profilu pacjenta
        </a>
    </div>

    <div class="report-header-card card border-0 shadow-lg text-light p-4 mb-4 rounded-4 position-relative overflow-hidden"
         style="background: linear-gradient(135deg, #1b1b22 0%, #17171c 100%); border: 1px solid rgba(70, 204, 94, 0.15) !important;">
        <div class="report-header-glow"></div>
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 position-relative z-1">
            <div class="d-flex align-items-center gap-3">
                <div class="rounded-circle d-flex align-items-center justify-content-center"
                     style="width:64px;height:64px;font-size:1.8rem;background:rgba(70, 204, 94, 0.15);border: 2px solid rgba(70, 204, 94, 0.4);">
                    <i class="bi bi-journal-check fs-2 text-success"></i>
                </div>
                <div>
                    <h2 class="mb-0 fw-bold">Wpis z dnia @Model.CreatedAt.ToString("dd.MM.yyyy")</h2>
                    <p class="mb-0 text-white-50 small">
                        <i class="bi bi-clock me-1"></i> Dokładna godzina: @Model.CreatedAt.ToString("HH:mm:ss")
                    </p>
                </div>
            </div>
            <div class="text-end">
                <div class="text-muted small text-uppercase fw-bold mb-2" style="letter-spacing: 0.5px;">Status wpisu</div>
                @if (Model.ReportStatus?.Name == "Zatwierdzony")
                {
                    <span class="badge bg-success text-white fs-6 px-3 py-2 rounded-pill shadow-sm">@Model.ReportStatus?.Name</span>
                }
                else if (Model.ReportStatus?.Name == "Odrzucony")
                {
                    <span class="badge bg-danger text-white fs-6 px-3 py-2 rounded-pill shadow-sm">@Model.ReportStatus?.Name</span>
                }
                else
                {
                    <span class="badge bg-secondary text-dark fs-6 px-3 py-2 rounded-pill shadow-sm">@Model.ReportStatus?.Name</span>
                }
            </div>
        </div>
    </div>

    <style>
        .report-header-glow {
            position: absolute;
            top: -50px;
            right: -50px;
            width: 250px;
            height: 250px;
            background: radial-gradient(circle, rgba(70, 204, 94, 0.1) 0%, transparent 70%);
            border-radius: 50%;
            z-index: 0;
            pointer-events: none;
        }

        .card {
            background-color: #1b1b22 !important;
            border: 1px solid rgba(255, 255, 255, 0.03) !important;
            transition: all 0.3s ease;
        }

        .card:hover {
            box-shadow: 0 10px 25px rgba(70, 204, 94, 0.15) !important;
            border-color: rgba(70, 204, 94, 0.2) !important;
        }

        .card-header {
            background: rgba(70, 204, 94, 0.05) !important;
            border-bottom: 1px solid rgba(70, 204, 94, 0.1) !important;
        }

        .btn-outline-primary {
            color: #46CC5E !important;
            border-color: rgba(70, 204, 94, 0.3) !important;
            transition: all 0.3s ease;
        }

        .btn-outline-primary:hover {
            background-color: rgba(70, 204, 94, 0.1) !important;
            border-color: #46CC5E !important;
            box-shadow: 0 0 15px rgba(70, 204, 94, 0.3);
        }

        .btn-outline-danger, .btn-outline-success {
            transition: all 0.3s ease;
        }

        .btn-outline-success:hover {
            box-shadow: 0 0 15px rgba(40, 167, 69, 0.4);
        }

        .btn-outline-danger:hover {
            box-shadow: 0 0 15px rgba(218, 41, 46, 0.4);
        }
    </style>

    <div class="row g-4">
        <div class="col-md-4">
            <div class="card border-0 shadow-sm mb-3" style="min-height: 205px;">
                <div class="card-header bg-dark-subtle border-bottom"
                     style="box-shadow: 0px 0px 10px 1px rgba(0, 0, 0, 0.55);">
                    <h6 class="mb-0 fw-bold">Pacjent</h6>
                </div>
                <div class="card-body" style="box-shadow: 0px 0px 10px 1px rgba(0, 0, 0, 0.55);">
                    <h5 class="fw-bold mb-1" style="margin-top: 13px;">@Model.AppUser?.FirstName
                        @Model.AppUser?.LastName</h5>
                        <p class="text-muted small mb-0">
                            <i class="bi bi-envelope me-1"></i> @Model.AppUser?.Email
                        </p>
                        <hr class="text-muted opacity-25" style="margin: 20px 0;" />
                        <div class="d-grid">
                            <a asp-action="PatientDetails" asp-route-id="@Model.AppUserId"
                               class="btn btn-outline-primary btn-sm">
                                <i class="bi bi-eye"></i> Pokaż pełny profil
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-md-8">
                <div class="card border-0 shadow-sm mb-3" style="min-height: 205px;">
                    <div class="card-header bg-black bg-opacity-25 border-bottom"
                         style="box-shadow: 0px 0px 10px 1px rgba(0, 0, 0, 0.55);">
                        <h6 class="mb-0 fw-bold">Odpowiedź pacjenta
                        </h6>
                    </div>
                    <div class="card-body p-4" style="box-shadow: 0px 0px 10px 1px rgba(0, 0, 0, 0.55); min-height: 166px;">
                        <p class="mb-0 lh-base text-dark">
                            <div class="d-flex justify-content-between align-items-center mb-1">
                                <small class="text-muted">
                                    <i class="bi bi-calendar3 me-1"></i>@Model.CreatedAt.ToString("dd.MM.yyyy HH:mm")
                                </small>
                            </div>
                            <p class="mb-0 small text-truncate" style="color: white;">@Model.Content</p>
                        </p>

                        @{
                            var attachment = Model.ReportAttachments?.FirstOrDefault();
                            bool isImage = attachment != null &&
                                (attachment.FilePath.EndsWith(".jpg", StringComparison.OrdinalIgnoreCase) ||
                                 attachment.FilePath.EndsWith(".jpeg", StringComparison.OrdinalIgnoreCase) ||
                                 attachment.FilePath.EndsWith(".png", StringComparison.OrdinalIgnoreCase));
                            string modalId = "attachmentModal_" + Model.Id.ToString("N");
                        }

                        @if (attachment != null)
                        {
                            <div class="mt-3 border-top pt-3">
                                <div class="text-muted small fw-bold mb-2">
                                    Załącznik:
                                </div>

                                @if (isImage)
                                {
                                    <div class="d-inline-flex align-items-center bg-dark border border-secondary rounded-3 p-2 shadow-sm mb-4"
                                         style="cursor: pointer; transition: background-color 0.2s;"
                                         onmouseover="this.style.backgroundColor='#2c2c2c'"
                                         onmouseout="this.style.backgroundColor=''" data-bs-toggle="modal"
                                         data-bs-target="#@modalId">
                                        <i class="bi bi-image text-primary fs-4 me-2"></i>
                                        <span class="text-light small fw-semibold me-3">
                                            @attachment.FileName
                                        </span>
                                        <i class="bi bi-eye text-muted small"></i>
                                    </div>

                                    <script>
                                                                (function () {
                                                                    var modalId = '@modalId';
                                                                    var filePath = '@attachment.FilePath';
                                                                    var fileName = '@attachment.FileName';

                                                                    var existing = document.getElementById(modalId);
                                                                    if (!existing) {
                                                                        var modalHtml = '<div class="modal fade" id="' + modalId + '" tabindex="-1" aria-hidden="true">' +
                                                                            '<div class="modal-dialog modal-dialog-centered modal-lg">' +
                                                                            '<div class="modal-content bg-transparent border-0 shadow-none">' +
                                                                            '<div class="text-end mb-2">' +
                                                                            '<button type="button" class="btn-close btn-close-white fs-4" data-bs-dismiss="modal" aria-label="Zamknij"></button>' +
                                                                            '</div>' +
                                                                            '<div class="modal-body p-0 text-center">' +
                                                                            '<img src="' + filePath + '" alt="Podgląd załącznika" class="img-fluid rounded-4 shadow-lg" style="max-height:80vh;object-fit:contain;" />' +
                                                                            '</div></div></div></div>';
                                                                        document.body.insertAdjacentHTML('beforeend', modalHtml);
                                                                    }

                                                                    var el = document.getElementById(modalId);
                                                                    var pageContainer = document.querySelector('.container.mt-4');
                                                                    el.addEventListener('show.bs.modal', function () {
                                                                        if (pageContainer) pageContainer.style.filter = 'blur(4px)';
                                                                    });
                                                                    el.addEventListener('hidden.bs.modal', function () {
                                                                        if (pageContainer) pageContainer.style.filter = '';
                                                                    });
                                                                })();
                                    </script>
                                }
                                else
                                {
                                    <a href="@attachment.FilePath" target="_blank"
                                       class="d-inline-flex align-items-center bg-dark border border-secondary rounded-3 p-2 shadow-sm text-decoration-none mb-4"
                                       style="transition: background-color 0.2s;"
                                       onmouseover="this.style.backgroundColor='#2c2c2c'"
                                       onmouseout="this.style.backgroundColor=''">
                                        <i class="bi bi-file-earmark-pdf text-danger fs-4 me-2"></i>
                                        <span class="text-light small fw-semibold me-3">@attachment.FileName</span>
                                        <i class="bi bi-box-arrow-up-right text-muted small"></i>
                                    </a>
                                }
                            </div>
                        }

                        <div class="d-flex justify-content-end gap-2 mt-auto border-top pt-3">
                            @if (Model.ReportStatus?.Name == "Oczekujący" || (Model.ReportStatus?.Name != "Zatwierdzony" &&
                                Model.ReportStatus?.Name != "Odrzucony"))
                            {
                                <form asp-action="RejectReport" asp-controller="Admin" method="post"
                                      onsubmit="return confirm('Czy na pewno chcesz odrzucić ten wpis? Punkty pacjenta zostaną cofnięte.');">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@Model.Id" />
                                    <button type="submit" class="btn btn-outline-danger">
                                        <i class="bi bi-x-circle fw-bold"></i> Odrzuć
                                    </button>
                                </form>

                                <form asp-action="ApproveReport" asp-controller="Admin" method="post">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@Model.Id" />
                                    <button type="submit" class="btn btn-outline-success px-4">
                                        <i class="bi bi-check-circle fw-bold"></i> Zatwierdź
                                    </button>
                                </form>
                            }
                        </div>

                    </div>
                </div>
            </div>
        </div>